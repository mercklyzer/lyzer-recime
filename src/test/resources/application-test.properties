# Flyway still runs in tests: the tests validate against the real migration,
# which is the only way a migration bug is caught before deploy.
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# Log SQL while developing the Specifications. Commented out for submission.
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
